<nav class="navbar navbar-expand-sm navbar-light bg-light">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-center" id="navbarSupportedContent">
      <ul class="navbar-nav">
        <li class="nav-item dropdown">
          <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">詳細</a>
          <ul class="dropdown-menu dropdown-menu-right">
            <li class="dropdown-diviver"></li>
            <li class="dropdown-item"><a href="<%= favorites_user_path(@user) %>" , class="nav-link">お気に入り<span class="badge badge-secondary"><%= @count_favorites %></span></a></li>
            <li class="dropdown-diviver"></li>
            <li class="dropdown-item"><a href="<%= followings_user_path(@user) %>", class="nav-link">フォロー <span class="badge badge-secondary"><%= @count_followings %></span></a></li>
            <li class="dropdown-diviver"></li>
            <li class="dropdown-item"><a href="<%= followers_user_path(@user) %>", class="nav-link">フォロワー <span class="badge badge-secondary"><%= @count_followers %></span></a></li>
            <li class="dropdown-diviver"></li>
            <% if current_user == @user %>
              <li class="dropdown-item"><a href="<%= root_path %>", class="nav-link">投稿する</a></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div>
　</nav>